// Middle of the Linked List
var middleNode = function (head) {
  //     let neww=[]
  //     let mat=Math.floor(head.length / 2)
  //   for(let i=mat;i<head.length;i++){
  // neww.push(head[i]);
  //   }
  //  return neww
  console.log(head.next);
};

middleNode([1, 2, 3, 4, 5, 6]);
